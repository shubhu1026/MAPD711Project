<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Transparent background for the corners -->
    <corners android:radius="16dp" /> <!-- Adjust the radius to control the curvature -->
    <stroke
        android:width="1dp"
        android:color="#000000" /> <!-- Optional: Add a stroke around the image -->
</shape>